#ifndef _BALLOT_H
#define _BALLOT_H

#include <iostream>
using namespace std;

#include "address.h"
#include "voter.h"
#include "citizen.h"
class Ballot
{
private:
	int id;
	Address address;
	Voter** voters;
	float voters_precent;
	int log;
	int phy;
	//results//
public: int getID();
		Address getAdress();
		Voter** getVoters();
		Voter getVoter(int i);
		float getVoters_percent();


		bool setID(int temp);
		bool setAddress(Address temp);
		bool CivCmp(Citizen a, Citizen b);
		bool increaseList(Voter** temp);
		bool addVoter(Citizen temp);
		bool openVotersListList();
		bool isInVoters(Citizen temp);
		float calculateVoterPercent();
};


#endif // !_BALLOT_H
