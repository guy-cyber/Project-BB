#ifndef _BALLOT_H
#define _BALLOT_H

#include <iostream>
using namespace std;

#include "address.h"
#include "voter.h"
#include "citizen.h"
class Ballot
{
private:
	int id;
	Address address;
	Voter** voters;
	int* sumVotes;
	float voters_precent;
	int logVoters;
	int phyVoters;
	int phySumVoters;
	//results//
public: int getID();
		Address getAdress();
		Voter** getVoters();
		Voter getVoter(int i);
		float getVoters_percent();
		int getSumVotes(int i);
		int getLogVoters();
		

		bool setPhySumVoters(int size);
		bool setID(int temp);
		bool setAddress(Address temp);
		bool increaseList(Voter** temp);
		bool addVoter(Citizen temp);
		bool openVotersListList();
		bool isInVoters(Citizen temp);
		float calculateVoterPercent();
		bool openSumVotes();
		bool addVote(int i);
};


#endif // !_BALLOT_H
